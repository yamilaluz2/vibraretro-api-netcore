/*public class Photo : Ifile
{
    public string GetPath()
    {
        string nombreArchivo = Guid.NewGuid().ToString() + Path.GetExtension(archivo.FileName);
        string pathCompleto = Path.Combine(ruta, nombreArchivo);
    }
}*/